import Chai from './Chai'; // always name is in capital letter.

function App() {
  return (
    
    <Chai/>
    // <h1> chai for refresh</h1> // this  is not working because we have to return only one element. so we have to wrap it in a div or in a react fragment.
  );
}
export default App;